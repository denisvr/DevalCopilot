namespace DevalCopilot.Application.Features.EnvironmentReadiness.Queries.GetProviderRuntimePreflight;

/// <summary>Closed identities for the local provider runtimes this preflight can observe.</summary>
public enum ProviderRuntime
{
    Codex = 0,
    ClaudeCode = 1,
}
